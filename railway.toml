# Сборка веб-минтера из папки minter (не blueprint в корне)
[build]
builder = "NIXPACKS"
buildCommand = "cd minter && npm ci && npm run build"

[deploy]
startCommand = "cd minter && npm run start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
