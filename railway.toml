# В Railway укажите Root Directory = minter
# См. minter/RAILWAY.md

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd minter && npm run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
